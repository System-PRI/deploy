# to run the application using docker, copy this file to server or your local machine and remove an 'example' suffix
# then set up the proper secrets values to the following variables

POSTGRES_URL=${POSTGRES_URL}
POSTGRES_USER=${POSTGRES_USER}
POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
POSTGRES_DB=${POSTGRES_DB_NAME}
JWT_SECRET=${JWT_SECRET}
MAIL_HOST=${MAIL_HOST}
MAIL_PORT=${MAIL_PORT}
MAIL_USERNAME=${MAIL_USERNAME}
MAIL_PASSWORD=${MAIL_PASSWORD}
EMAIL_UNIVERSITY_DOMAIN=${EMAIL_UNIVERSITY_DOMAIN}
LDAP_URL=${LDAP_URL}
LDAP_BASE=${LDAP_BASE}
LDAP_DOMAIN=${LDAP_DOMAIN}
FF_EMAIL_TO_UNIVERSITY_DOMAIN_ENABLED=${FF_EMAIL_TO_UNIVERSITY_DOMAIN_ENABLED}
FF_LDAP_AUTHENTICATION_ENABLED=${FF_LDAP_AUTHENTICATION_ENABLED}
SCHEDULED_JOBS_ENABLED=${SCHEDULED_JOBS_ENABLED}
